namespace SRPG
{
    using System;

    public enum PredefinedGlobalEvents
    {
        REFRESH_COIN_STATUS,
        REFRESH_GOLD_STATUS,
        UNLOAD_MENU,
        ERROR_NETWORK,
        BACK_NETWORK,
        MAINTENANCE_NETWORK,
        REFRESH_PLAYER_STATUS,
        ERROR_APP_QUIT,
        VERSION_MISMATCH_NETWORK
    }
}

