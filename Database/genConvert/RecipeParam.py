def RecipeParam(json):
    this={}#RecipeParamjson)
    #returnfalse
    if 'iname' in json:
        this['iname'] = json['iname']
    if 'cost' in json:
        this['cost'] = json['cost']
    #++length
    #if(length>0)
    #returntrue
return this
